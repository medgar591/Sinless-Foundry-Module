<section class="tab {{tabs.notes.cssClass}} full" data-group="primary" data-tab="notes">
	{{#if isEditable}} 
	<prose-mirror class="full" name="system.notes" data-document-u-u-i-d="{{actor.uuid}}" value="{{system.notes}}" collaborate="true" toggled="true">
      	{{{enrichedNotes}}}
	</prose-mirror>
	{{else}} {{{enrichedNotes}}}
	{{/if}}
</section>